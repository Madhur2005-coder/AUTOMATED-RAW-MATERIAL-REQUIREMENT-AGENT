services:
  - type: web
    name: raw-material-agent
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 22.14.0
      - key: OPENAI_API_KEY
        sync: false
